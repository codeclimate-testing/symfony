---
engines:
  duplication:
    enabled: false
  structure:
    enabled: true
  sonar-php:
    enabled: true
    config:
      minimum_severity: blocker
exclude_paths:
  - build/
  - fixtures/
  - gradle/
  - .gradle/
